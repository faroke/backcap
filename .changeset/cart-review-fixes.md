---
"@backcap/registry": patch
---

fix(registry): cart capability code review fixes — variantId+productId match, domain errors→400, price upper bound, $transaction required, Quantity.max stored, Cart.create validation
